"""awpack: install and manage agent packs.

Each pack declares what it is (id, version, status, runtime requirement) and
how to use it (install command). This package reads those declarations and
makes packs installable.
"""

__version__ = "0.1.0"
